package ch10;

public class Solution06 {

	/*Since the size of file is 20GB, we cannot read the data into memory at a time, so we have to divide the file
	 * into chunks, each chunk is K megabytes where K is the amount of the memory available. Each chunk is sorted 
	 * separately and then merged one by one, at the end, we have a fully sorted file.
	 */
}
